import React from 'react'

describe('Timer library', () => {
  // test("it should filter by a search term (link)", () => {
  //   expect(sum(2, 2)).toEqual(4);
  //   // expect(sum(2, 3)).toEqual(4);
  // });

  it('My Test Case', () => {
    expect(true).toEqual(true)
  })
})
